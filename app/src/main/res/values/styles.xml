<resources>

    <style name="ElevatorIndicatorText">
        <item name="android:layout_width">@dimen/floor_indicator_size</item>
        <item name="android:layout_height">@dimen/floor_indicator_size</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/elevator_floor_font</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/toolbar_text_size</item>
    </style>

</resources>
